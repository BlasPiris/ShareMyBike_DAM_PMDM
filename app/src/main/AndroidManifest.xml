<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sharemybike">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShareMyBike"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name="com.BlasPiris.sharemybike.activities.MainPanelActivity"
            android:exported="false"
            android:label="@string/title_activity_main_panel"
            android:theme="@style/Theme.ShareMyBike.NoActionBar" />
        <activity
            android:name="com.BlasPiris.sharemybike.activities.BikeActivity"
            android:exported="false"
            android:label="@string/title_activity_bike"
            android:theme="@style/Theme.ShareMyBike.NoActionBar" />
        <activity
            android:name="com.BlasPiris.sharemybike.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>